export default class dog {
    name; 
    old;


    constructor (name , old){
        this.name = name; 
        this.old = old; 
    }

    


};



export class horse {
    name; 
    old; 
    color;

    constructor (name, old, color) {
        this.name = name;
        this.old = old;
        this.color = color;
    }
}

export class cat {
    name; 
    color; 
    old; 
    sex; 

    constructor ( name, color, old, sex){
        this.name = name; 
        this.color = color; 
        this.old = old;
        this.sex = sex;
    }
};